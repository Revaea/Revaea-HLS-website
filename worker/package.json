{
  "name": "revaea-hls-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "pnpm -C ../frontend -s install --frozen-lockfile && pnpm -C ../frontend -s build && wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260214.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
